{
  "name": "futureway/futureway",
  "description": "MBTI + grade based faculty recommendation system for NRRU (PHP API, static front-end, Python decision tree)",
  "type": "project",
  "license": "MIT",
  "require": {
    "php": ">=8.1",
    "ext-mysqli": "*",
    "ext-mbstring": "*",
    "ext-json": "*"
  },
  "scripts": {
    "lint": "find php -name '*.php' -print0 | xargs -0 -n1 php -l"
  }
}
